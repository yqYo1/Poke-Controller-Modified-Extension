# Pytest CI temporarily disabled due to pygame/numpy Python 3.14 build issues
# TODO: Re-enable after upstream packages support Python 3.14

# name: Pytest
#
# on:
#   push:
#     branches: [main, master, refactor/rust-core]
#   pull_request:
#     branches: [main, master]
#
# jobs:
#   test-linux:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - uses: cachix/install-nix-action@v31
#         with:
#           github_access_token: ${{ secrets.GITHUB_TOKEN }}
#       - name: Sync dependencies
#         run: |
#           nix develop --command bash -c "uv venv && uv pip install pytest"
#       - name: Run pytest
#         run: |
#           nix develop --command pytest tests/ -v --tb=short
#
#   test-windows:
#     runs-on: windows-latest
#     strategy:
#       matrix:
#         python-version: ['3.14']
#     steps:
#       - uses: actions/checkout@v4
#       - uses: actions/setup-python@v5
#         with:
#           python-version: ${{ matrix.python-version }}
#       - name: Install uv
#         uses: astral-sh/setup-uv@v5
#       - name: Sync dependencies
#         run: uv sync --group dev
#       - name: Run pytest
#         run: uv run pytest tests/ -v --tb=short
